using UnityEngine;
using UnityEngine.UI;
using System;
using System.Collections;

public class PlayerParameter : MonoBehaviour {

	// Use this for initialization
	void Start () {

		//transform.FindChild ("STP").GetComponent<Text> ().text = PlayState.Instance.playerStatus.stp.ToString ();
		//transform.FindChild ("LV").GetComponent<Text> ().text = PlayState.Instance.playerStatus.lv.ToString();
	}
	
	// Update is called once per frame
	void Update () {
		//Debug.Log(PlayState.Instance.playerStatus.lv);
		//TimeSpan ts = DateTime.Now - PlayState.Instance.playerStatus.oldDateTime;
		//Debug.Log (ts);
	}
}
