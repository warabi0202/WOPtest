using UnityEngine;
using System.Collections;

public class test : MonoBehaviour {

	// Use this for initialization
	void Start () {
	}

	bool lateStartFlag = false;
	
	// Update is called once per frame
	void Update () {
		if (lateStartFlag == false) {
		}
	}
}
